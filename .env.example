POSTGRES_DB=spamira
POSTGRES_USER=spamira
# Set a unique password before starting Docker Compose.
POSTGRES_PASSWORD=
FRONTEND_PORT=3000
BACKEND_PORT=5080
ML_PORT=8000
POSTGRES_PORT=5432
VITE_API_BASE_URL=/api
# Use true when the public application is served over HTTPS.
AUTH_SECURE_COOKIES=false
NETWORK_SUBNET=172.30.250.0/24
FRONTEND_INTERNAL_IP=172.30.250.10
# Local development: set these variables in your shell before running the API.
# ConnectionStrings__Default=Host=localhost;Port=5432;Database=spamira;Username=spamira;Password=<your-password>;Timeout=5;Command Timeout=10
# MlService__BaseUrl=http://localhost:8000
# Database__AutoMigrate=true
# ASPNETCORE_URLS=http://localhost:5080
